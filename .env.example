# Cloudflare Pages / Functions environment variables
# Set these in the Cloudflare dashboard for your Pages project.
# Keep both false in production.
MOCK_MODE=false
ALLOW_MOCK_PRICES=false
CACHE_TTL_MINUTES=60
TICKETMASTER_DAILY_CAP=1000
TICKETMASTER_STALE_TTL_HOURS=168
CLICK_TRACKING_ENABLED=true
TICKETMASTER_DISCOVERY_ENABLED=true
TICKETMASTER_EVENTS_TTL_MINUTES=30
TICKETMASTER_ARTIST_EVENTS_LIMIT=100
TICKETMASTER_DISCOVERY_PRICE_CHECKS_ENABLED=true
# Optional two-letter filter for discovery results (e.g. US, GB, FR)
TICKETMASTER_DISCOVERY_COUNTRY=
# Optional override (default is Ticketmaster Discovery v2)
# TICKETMASTER_DISCOVERY_BASE_URL=https://app.ticketmaster.com/discovery/v2
# D1 binding (set in Cloudflare Pages/Workers bindings, not as a text env var):
# RATE_LIMIT_DB
# CLICKS_DB (optional dedicated D1 binding for click events)

# Ticketmaster
TICKETMASTER_API_KEY=your_key_here

# SeatGeek
SEATGEEK_CLIENT_ID=your_client_id_here
SEATGEEK_CLIENT_SECRET=your_client_secret_here

# Vivid Seats
VIVID_SEATS_API_KEY=your_key_here
