name: Ticketmaster Discovery Shell PR (Phase 2)

on:
  workflow_dispatch:
    inputs:
      score_threshold:
        description: "Minimum candidate score"
        required: false
        default: "70"
      max_shells:
        description: "Maximum shells per run (shell phase only; promote stays 1 per PR)"
        required: false
        default: "3"

jobs:
  shell_pr:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Download Phase 1 artifacts
        uses: actions/download-artifact@v4
        with:
          name: tm-discovery-proposal
          path: artifacts/tm-discovery

      - name: Create review_required shell PR (up to max_shells shells)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          TM_DISCOVERY_ARTIFACT_DIR: artifacts/tm-discovery
          TM_SHELL_SCORE_THRESHOLD: ${{ inputs.score_threshold }}
          TM_SHELL_MAX_PER_RUN: ${{ inputs.max_shells }}
        run: node scripts/tm-discovery-shell-pr.mjs
