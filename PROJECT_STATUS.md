# TourTicketCompare Project Status

Last updated: 2026-07-04 (**Data-accuracy correction** — a read-only audit recounted `public/data/events.json` directly and found this file's per-artist table and `needs_recheck` total had drifted since the 2026-07-02 refresh below: actual event totals are higher for 7 artists (e.g. `charli-xcx` now has 11 live events and `rosalia` 6, both previously documented as 0/artist-level-only), and `needs_recheck` events are actually **41**, not 22 — `bad-bunny` (8), `bts` (6), and `ariana-grande` (3) needs_recheck events were previously undocumented. Separately, 44 events across 7 artists (ariana-grande, charli-xcx, harry-styles, rosalia, bts, summer-walker, jay-z) carry a live/publishable `machine_high_confidence` status with **both** `tour_name` and `event_name` blank — this is a recurrence of the #172 pattern via newly-landed events, not the original artists' events (those remain correctly populated); root cause not confirmed in this audit but is consistent with the automated Ticketmaster new-show discovery pipeline landing rows before human tour-name review completes. Also fixed in this pass: three artist pages (`ed-sheeran`, `summer-walker`, `rosalia`) had stale `catalog.json` copy claiming "we do not show provider buttons yet" directly beside their live buttons — corrected to the same accurate copy used by `olivia-rodrigo`/`bruno-mars`, and two unused internal-only fields (`internal_notes`, `page_optimization`) were removed from the public `beyonce` catalog record. Per-artist table and counts below refreshed accordingly; see `BACKLOG.md` for the new active-priority item this created.)

Prior update: 2026-07-02 (**Affiliate pivot** — the site was removed from the Ticketmaster affiliate programme. Ticketmaster Impact machinery removed (Publisher Tag, evyy.net shortlinks, Impact wrapping); all Ticketmaster links are now plain, unmonetized verified redirects rendered after the affiliate providers. SeatGeek became the primary CTA: event CTAs inverted (SeatGeek primary, Ticketmaster secondary, standalone SeatGeek possible), plus **16 new artist-level `<slug>:seatgeek` entries** in `VERIFIED_TICKET_LINKS` (performer-page URLs captured from the SeatGeek `/2/performers/{id}` API for registry-verified performer ids; Impact-wrapped at click time) — `VERIFIED_TICKET_LINKS` is now **32 entries**, all 16 artists carry `verified_providers: ["ticketmaster","seatgeek"]`. Vivid Seats is fully wired on the Impact pattern but **dormant** until `IMPACT_VIVIDSEATS_*` secrets and verified `vividseats_url` data exist. Batch onboarding tooling landed (`artists:onboard:propose`, `artists:promote:batch`, up to 20 artists/PR with a human spot-check checklist). Owner follow-ups: browser-verify the 7 swapped plain TM URLs + 16 SeatGeek performer URLs, then delete the `IMPACT_TICKETMASTER_*` secrets in the Cloudflare dashboard.)

Prior update: 2026-06-17 (**ROSALÍA promoted** — onboarding complete. `rosalia` moved from `review_required` shell to `indexable_with_substantial_content` with a verified artist-level Ticketmaster CTA. Owner browser-confirmed the Ticketmaster artist page live 2026-06-17; the `redirectUrl` is the API-captured canonical URL from the Discovery attractions endpoint (attraction `K8vZ917pJy7`; storefront id `2453211` differs from the Discovery id, so API-sourced not name-built). `VERIFIED_TICKET_LINKS` now 16 entries; a verified `provider-identities.json` entry was added (required by the `validate:cta-provider-state` guard). 0 events — artist-level CTA only, like beyonce/raye/charli-xcx/tate-mcrae. All counts below refreshed; roster is now **16 artists, all indexable**.)

Prior update: 2026-06-16 (Roster grew to **16 artists**: `rosalia` (ROSALÍA) added as the first `review_required` Ticketmaster Discovery shell — no CTAs, no events, no `out.js` entry, noindex (PR #286). Onboarding her surfaced and fixed three discovery→promote pipeline bugs, all merged: the proposal feeder's HTTP 400 (`startDateTime` milliseconds), accented-name slugs (`ROSALÍA → rosal-a`, now `rosalia`), and URL provenance — discovery now captures the API-provided Ticketmaster URL and `promote-artist.mjs` sources it from there rather than constructing it from the artist name. Discovery is also US-scoped/depth-10/deduped now.)

Prior update: 2026-06-16 (Summer Walker `tour_name` populated as "Still Finally Over It" across all 10 events — owner-confirmed; partitions + events-index regenerated, `test:mvp` green. No blank-`tour_name` events remain. SeatGeek proposal run over the 83 uncovered future events returned zero candidates — all are European/non-US legs not listed on SeatGeek; no enrichment applied. Short-form URL recheck (owner, 2026-06-16): Ed Sheeran Glendale confirmed working in a browser and restored as `human_verified`; Nashville, Arlington, the Olivia Rodrigo 8 and the Shakira 12 re-confirmed 404 and stay suppressed. `needs_recheck` now 22. Count correction: body figures refreshed to **234/339 events carrying a stored `seatgeek_url`** (was 226) — PR #282 added 8 event-level SeatGeek CTAs (6 Summer Walker, 2 Shakira) that the prior doc refresh did not propagate into the counts.)

Prior update: 2026-06-15 (verification refresh — re-counted `public/data/*.json` and `functions/api/out.js` by direct inspection: 15 artists / 339 events / 226 with `seatgeek_url` / 23 `needs_recheck` / 10 Summer Walker blank `tour_name`. No drift since the 2026-06-12 update; figures below confirmed current, no content changes required. Prior update 2026-06-12: Ed Sheeran tour name "The Loop Tour" owner-confirmed and 24/27 event URLs restored as `machine_high_confidence`; counts and per-artist table re-verified by direct inspection of `public/data/` — the repo had drifted again since 2026-06-11: 10 Summer Walker events landed with CTAs live and blank `tour_name`, and 12 Shakira events became `needs_recheck` via PR #273)

This file is the current-state snapshot. Use `BACKLOG.md` for prioritised work and `CLAUDE.md` for protected areas, hard product rules, and validation. `docs/DOCS_MAINTENANCE.md` explains which files are canonical and how to keep this one fresh. Everything in `docs/archive/` is historical and should not be treated as current guidance unless referenced from here or `BACKLOG.md`.

## Runtime and architecture

- Production runtime: **Cloudflare Pages Functions**. Confirmed live via `/api/health` reporting `runtime: "cloudflare-pages-functions"`.
- Source of truth: GitHub `main`. Merges to `main` auto-deploy via Cloudflare Pages Git integration.
- HTML entry: `functions/_middleware.js` delegates non-asset, non-API requests to `functions/[[path]].js`.
- Page metadata source of truth: `functions/_route-metadata.js`.
- Named route shims (`functions/artists.js`, etc.) are fallback-only while middleware is active.
- D1 bindings: `DEMAND_DB` only. (No `RATE_LIMIT_DB`, no `CLICKS_DB`.)
- Impact bindings: `IMPACT_ACCOUNT_SID`, `IMPACT_AUTH_TOKEN` (network-level fallback), SeatGeek program bindings (`IMPACT_SEATGEEK_ACCOUNT_SID`, `IMPACT_SEATGEEK_AUTH_TOKEN`, `IMPACT_SEATGEEK_PROGRAM_ID`; server-side only), plus SeatGeek API credentials `SEATGEEK_CLIENT_ID` / `SEATGEEK_CLIENT_SECRET` (discovery tooling; not used by `/api/out`). **`IMPACT_TICKETMASTER_*` is no longer read by any code** — delete those secrets in the Cloudflare dashboard after the affiliate-pivot deploy is verified. `IMPACT_VIVIDSEATS_*` bindings are wired in code but not yet set. `MOCK_MODE=false`, `ALLOW_MOCK_PRICES=false`.
- Affiliate model (updated 2026-07-02): **Ticketmaster affiliate is gone** — the site was removed from the Ticketmaster affiliate programme. All Ticketmaster links are plain, unmonetized `ticketmaster.com` URLs (the Impact Publisher Tag `public/impact.js` and the `ticketmaster.evyy.net` shortlinks were removed; `/api/out` never calls Impact for Ticketmaster). SeatGeek (and Vivid Seats once configured) are the monetized providers via Impact.

## Current data

Verified by direct inspection of `public/data/` and `functions/api/out.js` on 2026-07-04:

- `public/data/artists.json`: **16 records — all 16 `indexable_with_substantial_content`**, all with `verified_providers: ["ticketmaster","seatgeek"]` (only 3 of the 16 — beyonce, raye, tate-mcrae — carry 0 events and render artist-level CTAs only; `charli-xcx` and `rosalia` now have live events, see table below).
- `public/data/catalog.json`: 16 artist records; 0 tour records; **32 ticket_links rows** (16 ticketmaster + 16 seatgeek artist pages); `providers.seatgeek.public_enabled: true`.
- `public/data/events.json`: **402 events**; **234 carry stored SeatGeek event URLs**; **41 are `needs_recheck`** (CTA-suppressed; up from a previously-documented 22 — see per-artist table); 0 carry `vividseats_url` yet.
- `public/data/events/<artist>.json`: per-artist partitions used at runtime.
- `public/data/guides-content.json`: **15 guide content entries** (topic guides; not per-artist).
- `functions/_route-metadata.js`: **15 guide routes** plus trust/static route metadata.
- `functions/api/out.js` `VERIFIED_TICKET_LINKS`: **32 entries** — one plain `<slug>:ticketmaster` and one Impact-wrapped `<slug>:seatgeek` (API-captured performer-page URL) per indexable artist. No Vivid Seats entries yet (wired but dormant).
- `data/provider-identities.json`: all 16 entries verified with `ticketmaster_attraction_id`, `ticketmaster_artist_url`, `seatgeek_performer_id`, and `seatgeek_artist_url`.

### Per-artist status

All 16 artists are `indexable_with_substantial_content` with `verified_providers: ["ticketmaster","seatgeek"]` and both `<slug>:ticketmaster` and `<slug>:seatgeek` entries in `VERIFIED_TICKET_LINKS`. (Table recounted directly from `events.json` on 2026-07-04 — the prior 2026-06-17 refresh had drifted for 7 artists; `last_verified_at` values are unchanged from `artists.json` and are not all current — see the note on each row.) Event counts from `events.json`:

| Slug | `last_verified_at` | Events | With `seatgeek_url` | `needs_recheck` | Tour name | Notes |
|---|---|---|---|---|---|---|
| beyonce | 2026-06-19 | 0 | 0 | 0 | — | No event records; artist-level CTA only. |
| harry-styles | 2026-04-30 | 39 | 30 | 0 | Together, Together | 9 events carry blank `tour_name`/`event_name` while still `machine_high_confidence` (CTA-live) — see Active risks. |
| bts | 2026-04-30 | 25 | 17 | **6** | BTS WORLD TOUR 'ARIRANG' | 2 events carry blank `tour_name`/`event_name` while `machine_high_confidence`; 6 `needs_recheck` (previously undocumented) — see Active risks. |
| ariana-grande | 2026-04-30 | 55 | 28 | **3** | The Eternal Sunshine Tour | 14 events carry blank `tour_name`/`event_name` while `machine_high_confidence`; 3 `needs_recheck` (previously undocumented) — see Active risks. |
| bad-bunny | 2026-04-30 | 32 | 0 | **8** | DeBÍ TiRAR MáS FOToS World Tour | No SeatGeek URLs yet. 8 `needs_recheck` (previously undocumented) — see Active risks. |
| morgan-wallen | 2026-04-30 | 18 | 16 | 0 | Still the Problem Tour | — |
| jay-z | 2026-04-30 | 4 | 3 | 0 | JAY-Z Yankee Stadium 2026 | 1 event carries blank `tour_name`/`event_name` while `machine_high_confidence` — see Active risks. |
| olivia-rodrigo | 2026-05-27 | 86 | 59 | **8** | The Unraveled Tour | 8 short-form event URLs human-checked 2026-06-10: all 404 in a browser; they remain `needs_recheck` with `ticketmaster_url` cleared and event CTAs suppressed. |
| bruno-mars | 2026-05-28 | 56 | 30 | 0 | The Romantic Tour | Four Mexico City events intentionally excluded (`ticketmaster.com.mx` not in the Ticketmaster host allowlist). |
| ed-sheeran | 2026-06-12 | 27 | 27 | **2** | The Loop Tour | Tour title owner-confirmed 2026-06-12; owner spot-checked the long-form `loop-tour` storefront URLs in a browser. 24 events `machine_high_confidence` with CTAs live. Glendale short-form `/event/Z7r9jZ1A7j-_M` URL owner-confirmed working 2026-06-16 and restored as `human_verified` (25/27 now publishable). **2 short-form `/event/<id>` URLs (Nashville, Arlington) re-checked 2026-06-16 — still 404, stay `needs_recheck` with URLs cleared and CTAs suppressed.** `ticket_buying_notes`/FAQ copy fixed 2026-07-04 (was contradicting the live CTAs). |
| shakira | 2026-06-10 | 30 | 18 | **12** | Las Mujeres Ya No Lloran | Tour title owner-confirmed from the Ticketmaster event page 2026-06-10. 12 events `needs_recheck` with cleared `ticketmaster_url` (CTAs suppressed) since PR #273. |
| raye | 2026-06-19 | 0 | 0 | 0 | — | No event records; artist-level CTA only. |
| charli-xcx | 2026-06-18 | 11 | 0 | 0 | — | **Corrected 2026-07-04: this artist now has 11 live events, all `machine_high_confidence`, all with blank `tour_name`/`event_name`** — was previously (incorrectly) documented as 0 events/artist-level-only. See Active risks. |
| tate-mcrae | 2026-06-19 | 0 | 0 | 0 | — | No event records; artist-level CTA only. |
| summer-walker | 2026-06-11 | 13 | 6 | **1** | Still Finally Over It | Tour name owner-confirmed 2026-06-16 for the original 10 events; 3 additional events have since landed with blank `tour_name`/`event_name` (2 `machine_high_confidence`, 1 `needs_recheck`) — see Active risks. |
| rosalia | 2026-06-18 | 6 | 0 | **1** | — | **Corrected 2026-07-04: this artist now has 6 events (5 `machine_high_confidence`, 1 `needs_recheck`), all with blank `tour_name`/`event_name`** — was previously (incorrectly) documented as 0 events/artist-level-only. `ticket_buying_notes`/FAQ copy fixed 2026-07-04 (was contradicting the live CTAs). See Active risks. |

Note on `needs_recheck` rendering (updated 2026-07-02): event CTAs render independently per provider. The SeatGeek CTA is primary ("View tickets on SeatGeek"), the plain Ticketmaster link secondary ("Check Ticketmaster"), and either renders standalone. A `needs_recheck` event suppresses both CTAs **unless** the SeatGeek link carries its own verified provenance (`provider_links.seatgeek.verified === true` — currently zero events, so all 41 recheck events still show "No verified ticket link is available for this date", the safe state). The shared rule is `providerEventPublishable` in `functions/api/out.js`, `functions/[[path]].js`, `public/app.js`, and `functions/api/shows.js`.

## Validation pipeline

Run before committing data, content, or rendering changes:

- `python3 scripts/validate-events.py --for-production` — event schema, hard error on missing `tour_name` key, warning on blank `tour_name` for indexed artists (PR #186).
- `node scripts/validate-guide-routes.mjs` — guide route / content / sitemap drift validation (PR #184).
- `node scripts/validate-artist-provider-claims.mjs` — artist metadata vs `VERIFIED_TICKET_LINKS` drift guard (PR #185).
- `node scripts/validate-cta-provider-state.mjs` (`npm run validate:cta-provider-state`, in `test:mvp`) — read-only CTA ↔ provider-state guard: artist CTAs backed by a `verified` registry identity, no `withheld` identity publishing, every publishable event resolvable through `/api/out`, every `machine_high_confidence` row meeting its canonical contract. Runtime gates unchanged.
- `npm run artist:check -- <slug>` — per-artist readiness validator: checks `artists.json`, `catalog.json`, `events.json`, partition files, `VERIFIED_TICKET_LINKS` in `out.js`, and the `shows.js` affiliate map (PR #188; since 2026-06-11 the shows.js map is derived from out.js and the signup allowlist from artists.json — neither is hand-edited per artist).
- `node scripts/smoke-prelaunch.mjs` — route/CTA/copy smoke checks.
- `npm run seatgeek:self-test` — SeatGeek discovery scoring/safety smoke test (no API calls). Run before changing the discovery tooling.
- `npm run providers:identities:validate` — provider identity registry validation (`data/provider-identities.json` vs `artists.json` and the out.js Ticketmaster host allowlist). Run whenever the registry changes. See `docs/PROVIDER_SYNC.md`.
- `node --check public/app.js`, `node --check 'functions/[[path]].js'`, `node --check functions/api/out.js` — syntax.
- `git diff --check` — whitespace and conflict markers.

`npm run test:mvp` runs the events self-test, artist-provider validator, and smoke suite together.

## Daily automation

`.github/workflows/daily-audit.yml` runs at 03:00 UTC and on `workflow_dispatch`. It performs:

1. URL liveness via `scripts/verify-outbound-links.mjs`.
2. Ticketmaster Discovery diff via `scripts/audit-tm-events.mjs` (requires `TICKETMASTER_API_KEY`; skipped safely if absent). The Discovery API is keyed by the **Discovery event id** (`ticketmaster_discovery_event_id` / `provider_links.ticketmaster.discovery_event_id`), NOT the consumer-website `/event/<id>` code stored in `ticketmaster_event_id` (16-char hex) or the international numeric storefront id. Events lacking a Discovery-format id are reported as **`unresolvable`** (surfaced for backfill, **not** counted as missing). `scripts/backfill-discovery-ids.mjs` (`npm run audit:backfill-discovery-ids`, dry-run by default; `--apply` to write) recovers Discovery ids from the verified per-artist attraction feed by venue-local-date + city match, writing only on unambiguous API matches. Date/venue/status comparison is timezone-aware and only flags actionable status transitions (cancelled/postponed/rescheduled).
3. Reporting via `scripts/daily-audit-report.mjs` into a single rolling GitHub issue (`automation:daily-audit`).
4. Verification-date bumps via `scripts/bump-verified-dates.mjs`, opening a PR for human review. The TM-skip/failure guard added in PR #182 prevents date bumps when TM data is unavailable.

`.github/workflows/tm-new-shows-pr.yml` runs at 04:00 UTC and on `workflow_dispatch`. It is the scheduled **new-show discovery loop** (provider-sync step 3, write-to-PR mode): it runs the Ticketmaster Discovery recogniser for every verified, `sync_enabled` artist via `scripts/sync-tm-events-write-pr.mjs` and opens **one review PR** (`automation:tm-events` label) proposing newly-discovered shows. It **never commits to `main` and never auto-merges**. The recogniser withholds events already in `events.json`, so only genuinely new shows are proposed and a quiet day produces no commit and no PR; risky rows are withheld for human review; `tour_name` is left blank for human verification (#172). Scheduled runs always open the PR; manual `workflow_dispatch` defaults to a safe `preview` (no PR) and accepts an optional single-artist `artist` input. Without `TICKETMASTER_API_KEY` the recogniser no-ops safely (no rows, no PR). See `docs/PROVIDER_SYNC.md`.

`.github/workflows/nightly-data-sync.yml` is currently **manual-only**: the historical 03:30 UTC cron is commented out and must not be re-enabled without clean manual-run evidence. Manual runs default to `dry_run: true`; a dry-run writes `.audit/tm-sync.json` as an uploaded artifact and cannot commit or push. A non-dry-run can **auto-commit to `main`** only for lossless factual updates (date/time, venue/city, refreshed canonical TM URL) to events that already exist in `events.json`, pulled per event id from the Ticketmaster Discovery API via `scripts/apply-tm-updates.mjs`, then regenerates the inline fallback and partitions. The commit is blocked by any review item, blocked update, error, missing report, dry-run input, validation failure, smoke-test failure, or absent `events.json` diff. Items that need human judgement — new shows, deletions (404/410), cancelled/postponed status, `tour_name` — are **never auto-applied**; they are surfaced in the rolling `automation:data-sync` issue via `scripts/report-tm-sync-review.mjs`. See `docs/DEPLOYMENT.md` → "Nightly data sync operational state" and `SAFE_PUBLISHING_RULES.md` → "Discovery, Enrichment, and Rendering" for the boundary. (`TICKETMASTER_API_KEY` is required for useful live checks; if absent, the run writes a skipped report and no-ops safely.)

`.github/workflows/prelaunch-validation.yml` includes a `stale-sync-guard` that runs `npm run events:sync` on PRs touching `public/data/*.json` and fails if `public/index.html` is stale.

## Active risks

These are the live risks. Detailed task scope and ordering live in `BACKLOG.md`.

- **Affiliate-pivot owner follow-ups (2026-07-02, pre/post-merge).** Before merge: browser-verify the 7 swapped plain Ticketmaster artist URLs (beyonce, harry-styles, bts, ariana-grande, bad-bunny, morgan-wallen, jay-z — canonical URLs from the verified registry) and the 16 SeatGeek performer-page URLs (API-captured; listed in `data/provider-identities.json` `seatgeek_artist_url`). After deploy: confirm `/api/out?artistSlug=<slug>&provider=ticketmaster` 302s plain and `provider=seatgeek` 302s to the Impact tracking URL, confirm no `utt.impactcdn.com` requests in devtools, then **delete the unused `IMPACT_TICKETMASTER_*` secrets** in the Cloudflare dashboard (keep `IMPACT_ACCOUNT_SID`/`IMPACT_AUTH_TOKEN`). To activate Vivid Seats: add `IMPACT_VIVIDSEATS_*` (or `IMPACT_VIVIDSEATS_BASE_TRACKING_URL`) secrets and land verified `vividseats.com` destinations.

- **Daily TM audit — false "missing" storm fixed; residual real findings.** The audit was reporting ~314/339 events "missing (404/410)" because it queried the Discovery API with the website `/event/<id>` hex code / international numeric id instead of the Discovery event id. Fixed: the audit now keys on `ticketmaster_discovery_event_id`, and 218 events were backfilled with verified Discovery ids (`scripts/backfill-discovery-ids.mjs`). Post-fix the diff is **0 missing, 4 changed, 50 unresolvable**. The **4 changed** are genuine venue-label discrepancies (Olivia Rodrigo Amsterdam 2027: local "Vinyl Room - Ziggo Dome" vs TM "Ziggo Dome Club") — review and correct the local label or accept TM's. The **50 unresolvable** are European/`announced` shows not surfaced by id in the US Discovery feed (mostly Bad Bunny EU, BTS, Morgan Wallen announced dates); they carry verified storefront URLs and are CTA-safe — left without a Discovery id rather than guessing. NB: in CI the link-liveness half still returns ~all WAF-blocked, and the conservative blocked-link guard holds date bumps regardless — that is intentional (commit "Keep date-bump guard conservative for blocked links") and unchanged here.

- **Short-form `ticketmaster.com/event/<id>` URLs remain broken across three artists — 2 Ed Sheeran, 8 Olivia Rodrigo, 12 Shakira events CTA-suppressed.** Owner short-form recheck 2026-06-16: Ed Sheeran **Glendale** confirmed working in a browser and restored as `human_verified` (short-form URL passes the runtime gate and the publishable redirect invariant; `human_verified` is exempt from the `machine_high_confidence` canonical long-form contract). Ed Sheeran **Nashville** and **Arlington**, the **Olivia Rodrigo 8**, and the **Shakira 12** were all re-confirmed 404 and stay `needs_recheck` with URLs cleared. The Discovery API still resolves the same IDs (status `onsale`) despite the browser 404s. Suppression is the correct safe state. Re-check periodically for working storefront URLs; do not restore from the Discovery `url` field alone. **Upgrade path for Glendale:** if the owner supplies the canonical long-form storefront URL the short-form redirects to, it can be promoted to `machine_high_confidence`. **`needs_recheck` is now 41 total, not 22** — see the next item for the other 19 (bad-bunny 8, bts 6, ariana-grande 3, plus 1 each summer-walker/rosalia), which are a distinct, more recent batch.
- **Blank `tour_name`/`event_name` has recurred on newly-landed events (#172 was resolved for the *original* affected artists, but new events keep landing blank) — found 2026-07-04, not yet actioned.** The original #172 fix stands: Olivia Rodrigo (86, "The Unraveled Tour"), Bruno Mars (56, "The Romantic Tour"), Shakira (30, "Las Mujeres Ya No Lloran"), Ed Sheeran's original 27, and Summer Walker's original 10 events are all still correctly populated. But `python3 scripts/validate-events.py --for-production` now warns on **63 events across 8 artists** with blank `tour_name` (ariana-grande 17, harry-styles 9, charli-xcx 11, bts 8, bad-bunny 8, rosalia 6, summer-walker 3, jay-z 1) — of which **44 are also missing `event_name` and are in the live, CTA-eligible `machine_high_confidence` state** (not suppressed): ariana-grande 14, charli-xcx 11, harry-styles 9, rosalia 5, bts 2, summer-walker 2, jay-z 1. `charli-xcx` (11 events) and `rosalia` (6 events) are new entirely — both were previously documented in this file as 0-event, artist-level-CTA-only artists. Root cause not confirmed in this read-only audit, but the pattern (blank `tour_name`, new events, several already `machine_high_confidence`) is consistent with the automated Ticketmaster new-show discovery pipeline (`.github/workflows/tm-new-shows-pr.yml`) landing rows ahead of the human tour-name review it's designed to require. **Needs owner action:** supply real tour names for these 44+ events (never infer from URL slugs, per `SAFE_PUBLISHING_RULES.md`), or explicitly approve flipping them to `needs_recheck` in the interim. Also worth checking whether the daily-audit / new-show-PR review step is being followed before these land as `machine_high_confidence`.
- **Data refresh documentation (#174).** `scripts/sync-events-data.py` inlines fallback data into `public/index.html`; the `stale-sync-guard` CI job catches stale commits on PRs. Phase A (documented data-refresh flow) is addressed in `docs/DEPLOYMENT.md`. Phase B (optional cache-bust scheme) remains parked.
- **Stale-file risk (#176) — deletions done 2026-06-19.** The Vercel deploy artefacts (`api/`, `vercel.json`) and the legacy standalone Worker builder (`scripts/build-standalone-worker.mjs`) were deleted (candidates #1–#3) after owner authorisation; the stale `_route-metadata.js` comment was removed and the canonical docs updated. `npm run test:mvp` green. Remaining (intentionally kept): the inactive route shims (#5, documented fallback) and `archive/vercel-experimental/README.md` (#4, historical marker). Evidence in `docs/STALE_FILE_AUDIT.md`.
- **Raw HTML production proof (#10).** Local proof passed (17 representative routes, 2026-05-19); production browser proof is still unconfirmed. PR #184's guide drift validation reduces the risk further. This is not a coding priority unless a fresh production check identifies a real mismatch.
- **Status-doc drift.** This file drifted twice: 2026-06-03→11 (5 artists, 57 events) and again 2026-06-11→12 (10 summer-walker events with live CTAs, 12 shakira events suppressed via PR #273 — neither was reflected here until the 2026-06-12 refresh). When merging data PRs, refresh the counts here (see `docs/DOCS_MAINTENANCE.md` → update triggers).

## Product guardrails (summary)

The full rules are in `docs/CONTENT_RULES.md`, `docs/PROVIDER_DATA_POLICY.md`, and `CLAUDE.md`. Highlights:

- Never invent tours, dates, venues, prices, availability, providers, inventory, or ticket links.
- Never scrape ticket providers.
- Never claim live price comparison unless approved provider feeds support it.
- Never expose Impact or any other secret client-side.
- CTAs only when artist ∈ catalog, provider has a verified `redirectUrl`, and the URL passes `/api/out` validation.

## What is supported today

- Homepage and trust/legal pages.
- Artist index plus all 16 indexable artist pages above (5 of them — beyonce, raye, charli-xcx, tate-mcrae, rosalia — currently have no event records and render artist-level CTAs only).
- 15 guide pages with server-rendered content.
- Verified SeatGeek CTAs, **artist-level and event-level**, as the primary CTA — routed through `/api/out` with Impact tracking (base tracking URL or API mint). Artist-level entries are performer-page URLs captured from the SeatGeek `/2/performers/{id}` API for registry-verified performer ids. Currently 234/400 events carry a stored `seatgeek_url`; discovery tooling (`npm run seatgeek:propose` / `seatgeek:enrich:apply`) remains operational; see `docs/SEATGEEK_DISCOVERY.md`.
- Verified plain Ticketmaster links (artist- and event-level) — unmonetized, rendered after the affiliate providers ("Check Ticketmaster" / "View tickets" when alone). No Impact wrapping, no Publisher Tag, no evyy.net shortlinks.
- Vivid Seats wiring (impactConfig branch, event + artist paths, rendering slots, `vividseats_url` validation) — **dormant**: no CTAs render until `IMPACT_VIVIDSEATS_*` (or a pxf.io base tracking URL) is set and verified destinations exist.
- Batch onboarding tooling: `npm run artists:onboard:propose` (API-captured SeatGeek/TM identity manifest) and `npm run artists:promote:batch` (up to 20 shells per PR, per-artist human browser spot-check checklist, dry-run by default).
- First-party analytics and signup writes through `DEMAND_DB`.
- Provider-sync **sequence complete (steps 1–5)** — registry + validators, Ticketmaster dry-run recognition, Ticketmaster write-to-PR mode (`scripts/sync-tm-events-write-pr.mjs`), SeatGeek performer-id-scoped enrichment dry-run, and the read-only CTA ↔ provider-state guard (`scripts/validate-cta-provider-state.mjs`). All write paths stay human-gated/PR-based; runtime CTA gates and `/api/out` are unchanged. No autonomous provider writes to `main`. Remaining work is operational (human populates registry IDs, runs the gated tools). See `docs/PROVIDER_SYNC.md` and `BACKLOG.md` item 5.

## What is not supported

- Live multi-provider price aggregation; "cheapest" / "guaranteed availability" claims (including any SeatGeek price display).
- Tour, city, venue, or event landing pages.
- Public Vivid Seats CTAs — **wired but not live**: the full code path exists (2026-07-02) but no `IMPACT_VIVIDSEATS_*` secrets and no verified `vividseats.com` destinations exist yet; buttons stay hidden until both do.
- Ticketmaster affiliate/tracking of any kind (the site is no longer in the Ticketmaster affiliate programme).
- `Event` / `MusicEvent` schema on any page without verified event-level data.

## How to update this file

Refresh when any of the following change: artist count, indexing status, event count, guide count, bindings, validation pipeline, daily automation, or the set of active issues. Recount from the data files (`python3 -c` over `public/data/*.json`) rather than trusting prior text — this file drifted once already. Reference `BACKLOG.md` for prioritised work — do not duplicate the priority list here. See `docs/DOCS_MAINTENANCE.md` for the canonical-file map.
