# TourTicketCompare Project Status

Last updated: 2026-07-16 (agent: full recount from source files after PRs #472–#484 merged. PR #472 added 22 events — post-malone 5, zach-bryan 15, plus 1 shakira and 1 bad-bunny `needs_recheck` row that re-added shows deleted in the 2026-07-15 dedup — taking events 380→402 and `needs_recheck` 13→20. PRs #473–#479 reworked `/api/shows`: normal traffic now reads the persisted catalogue and live Ticketmaster artist discovery is an opt-in runtime flag (`TICKETMASTER_LIVE_ARTIST_DISCOVERY_ENABLED`, default off) backed by the `TICKETMASTER_API_KEY` Pages secret. PR #480 scheduled the Impact marketplace event-link sync nightly with auto-merge, owner-approved. The 2026-07-16 scheduled CTA syncs (#482 SeatGeek, #483 Vivid Seats, #484 TicketNetwork) auto-merged.) The roster remains 20 artists — 18 indexable + 2 `review_required` shells (sabrina-carpenter, lady-gaga, held pending live dates). The cache-only price architecture is active: dedicated Vivid Seats and shared Impact marketplace workflows write approved exact-event observations to D1, and `/api/shows` serves only lanes whose provider, URL, provenance, source, feature-flag, timestamp, and expiry gates pass. Vivid Seats, TicketNetwork, and StubHub International have active numeric-price lanes; Ticket Liquidator remains price-disabled because its catalog has no numeric `CurrentPrice`. SeatGeek carries no numeric-price lane: its API returns null pricing statistics for this client and (owner-confirmed 2026-07-15) never will, so SeatGeek price snapshots are permanently disabled — not a pending entitlement (see Active risks). Earlier update history lives in git (`git log -- PROJECT_STATUS.md`).

This file is the current-state snapshot—the **only** place live state (counts, per-artist status, active risks) is recorded. Use `BACKLOG.md` for prioritised work and `CLAUDE.md` for protected areas, hard product rules, and validation. `docs/DOCS_MAINTENANCE.md` explains ownership and drift checks. Superseded material lives in git history, not in parallel archive or handover documents.

## Runtime and architecture

- Production runtime: **Cloudflare Pages Functions**. Confirmed live via `/api/health` reporting `runtime: "cloudflare-pages-functions"`.
- Source of truth: GitHub `main`. Merges to `main` auto-deploy via Cloudflare Pages Git integration.
- HTML entry: `functions/_middleware.js` delegates non-asset, non-API requests to `functions/[[path]].js`.
- Page metadata source of truth: `functions/_route-metadata.js`.
- Named route shims (`functions/artists.js`, etc.) are fallback-only while middleware is active.
- D1 bindings: `DEMAND_DB` only. (No `RATE_LIMIT_DB`, no `CLICKS_DB`.)
- `/api/shows` serves artist pages from the persisted, scheduled catalogue (PR #479). Live Ticketmaster artist discovery exists as an opt-in operations path only: it requires both the `TICKETMASTER_LIVE_ARTIST_DISCOVERY_ENABLED` runtime flag (default false) and an explicit request parameter, uses the `TICKETMASTER_API_KEY` Pages secret (configured 2026-07-15, PR #475), retains the last verified response for 36h, and serves it on Ticketmaster 429/upstream errors. Normal visitor traffic never triggers a Discovery API call.
- Impact bindings: network-level and provider-specific credentials are server-side only. SeatGeek, Vivid Seats, TicketNetwork, Ticket Liquidator, and StubHub International event lanes require runtime configuration and verified destinations; SeatGeek also has artist-level entries. SeatGeek API credentials are used only by controlled discovery/snapshot tooling. **`IMPACT_TICKETMASTER_*` is no longer read by code** and should be removed from Cloudflare. `MOCK_MODE=false` and `ALLOW_MOCK_PRICES=false`; every display lane remains source/exact-event/freshness gated.
- Affiliate model: **Ticketmaster affiliate is gone**—Ticketmaster redirects are plain and unmonetized. All affiliate-provider lanes fail closed when provider configuration, URL/provenance validation, or Impact tracking is unavailable.

## Current data

Verified by direct inspection of `public/data/` and `functions/api/out.js` on 2026-07-16 (`npm run data:stats` + direct recount):

- `public/data/artists.json`: **20 records — 18 `indexable_with_substantial_content` + 2 `review_required` shells** (sabrina-carpenter, lady-gaga). The 18 indexable artists carry `verified_providers: ["ticketmaster","seatgeek"]`; the 2 shells carry `verified_providers: []` and render no CTA. 3 of the 18 indexable — beyonce, raye, tate-mcrae — carry 0 events and render artist-level CTAs only.
- `public/data/catalog.json`: 20 artist records; 0 tour records; **38 ticket_links rows** (20 ticketmaster + 18 seatgeek artist pages; 36 `verified` + `public_enabled`, plus 2 unverified/hidden shell ticketmaster rows for the `review_required` artists); the `seatgeek` provider entry has `public_enabled: true`.
- `public/data/events.json`: **402 events** (PR #472 added 22 on 2026-07-15: post-malone 5, zach-bryan 15, shakira 1, bad-bunny 1 — see Active risks for the 2 re-added dedup rows). Verification status: 284 `human_verified`, 98 `machine_high_confidence`, 20 `needs_recheck`. 250 carry stored SeatGeek event URLs and 198 carry verified SeatGeek provenance; 222 carry verified Vivid Seats event URLs/provenance; 198 carry verified TicketNetwork, 156 verified Ticket Liquidator, and 200 verified StubHub International provenance. Of the **20 `needs_recheck`** rows: 13 retain a standalone SeatGeek CTA, 0 retain a standalone Vivid Seats CTA, and 7 have no independently verified resale provider and remain fully CTA-suppressed.
- `public/data/events/<artist>.json`: per-artist partitions used at runtime.
- `public/data/guides-content.json`: **17 guide content entries** (topic guides; not per-artist).
- `functions/_route-metadata.js`: **17 guide routes** plus trust/static route metadata.
- `functions/api/out.js` `VERIFIED_TICKET_LINKS`: **36 artist-level entries** — one plain `<slug>:ticketmaster` and one Impact-wrapped `<slug>:seatgeek` per indexable artist (18 artists). There are no artist-level Vivid Seats entries; live event-level Vivid redirects resolve from verified event data.
- `data/provider-identities.json`: all **18 entries** verified with `ticketmaster_attraction_id`, `ticketmaster_artist_url`, `seatgeek_performer_id`, and `seatgeek_artist_url` (the 2 `review_required` shells have no registry entry yet — added at Promote).

### Per-artist status

18 of the 20 artists are `indexable_with_substantial_content` with `verified_providers: ["ticketmaster","seatgeek"]` and both `<slug>:ticketmaster` and `<slug>:seatgeek` entries in `VERIFIED_TICKET_LINKS`; the remaining 2 (sabrina-carpenter, lady-gaga) are `review_required` shells with no CTA (last two rows below). (Table recounted directly from `events.json` on 2026-07-16; `last_verified_at` values are unchanged from `artists.json` and are not all current — see the note on each row.) "SG verified" = events carrying `provider_links.seatgeek.verified === true`. Event counts from `events.json`:

| Slug | `last_verified_at` | Events | With `seatgeek_url` | SG verified | `needs_recheck` | Tour name | Notes |
|---|---|---|---|---|---|---|---|
| beyonce | 2026-06-19 | 0 | 0 | 0 | 0 | — | No event records; artist-level CTA only. |
| harry-styles | 2026-04-30 | 39 | 30 | 30 | 0 | Together, Together | Blank-title backfill 2026-07-06: all 39 events now carry `tour_name`/`event_name`. |
| bts | 2026-04-30 | 25 | 21 | 14 | **4** | BTS WORLD TOUR 'ARIRANG' | Owner rechecks 2026-07-15: Munich 7/11 & 7/12 storefront URLs confirmed working and restored; Madrid 6/26 & 6/27 and Arlington 8/16 & 8/17 confirmed dead and cleared (4 remain `needs_recheck`; the 2 Arlington rows keep a standalone SeatGeek CTA). |
| ariana-grande | 2026-04-30 | 41 | 24 | 12 | 0 | The Eternal Sunshine Tour | 13 duplicate rows removed 2026-07-15 (54→41 events); the surviving `human_verified` rows kept all SeatGeek/Vivid coverage (earlier verified counts double-counted the dupes). The 3 Sunrise rows were owner-verified (page loads; not on sale via TM) and restored with a plain "Check Ticketmaster" link. All 41 events carry `tour_name`/`event_name`; 0 `needs_recheck`. |
| bad-bunny | 2026-04-30 | 26 | 0 | 0 | **2** | DeBÍ TiRAR MáS FOToS World Tour | No SeatGeek URLs (EU legs not listed on SeatGeek). Owner rechecks 2026-07-15: Düsseldorf 6/20 & 6/21 and Brussels 7/21 were dead `.com` duplicates of already-published `human_verified` `.de`/`.be` rows and were deleted (28→25); Marseille 7/1 confirmed dead and cleared. PR #472 then re-added a Brussels `.com` row as `needs_recheck` (25→26; CTA-suppressed — see Active risks). |
| morgan-wallen | 2026-04-30 | 18 | 16 | 6 | 0 | Still the Problem Tour | — |
| jay-z | 2026-04-30 | 5 | 3 | 3 | 0 | JAY-Z Yankee Stadium 2026 | The Inglewood SoFi (`event_name` "JAY-Z30") and London Tottenham Hotspur Stadium (`event_name` "JAY-Z - 30") rows have blank `tour_name` values and need owner labels. |
| olivia-rodrigo | 2026-05-27 | 84 | 59 | 59 | **6** | The Unraveled Tour | 8 short-form event URLs were human-checked 2026-06-10 and returned 404 in a browser; 6 current rows remain `needs_recheck` with `ticketmaster_url` cleared, and all 6 retain a standalone SeatGeek CTA via verified provenance (PR #356). The wrong-night Sunrise/Las Vegas SeatGeek URL shares were corrected in the same run. |
| bruno-mars | 2026-05-28 | 56 | 30 | 30 | 0 | The Romantic Tour | Four Mexico City events intentionally excluded (`ticketmaster.com.mx` not in the Ticketmaster host allowlist). Falcon Stadium wrong-night SeatGeek URL share corrected by PR #356. |
| ed-sheeran | 2026-06-12 | 27 | 27 | 22 | **2** | The Loop Tour | Tour title owner-confirmed 2026-06-12; owner spot-checked the long-form `loop-tour` storefront URLs in a browser. 24 events `machine_high_confidence` with CTAs live. Glendale short-form `/event/Z7r9jZ1A7j-_M` URL owner-confirmed working 2026-06-16 and restored as `human_verified` (25/27 now publishable). **2 short-form `/event/<id>` URLs (Nashville, Arlington) re-checked 2026-06-16 — still 404, stay `needs_recheck` with URLs cleared**; 1 of the 2 regained a standalone SeatGeek CTA via verified provenance (PR #356). `ticket_buying_notes`/FAQ copy fixed 2026-07-04 (was contradicting the live CTAs). |
| shakira | 2026-06-10 | 31 | 17 | 6 | **1** | Las Mujeres Ya No Lloran | Tour title owner-confirmed 2026-06-10. Owner rechecks 2026-07-15: all 12 Madrid residency storefront URLs confirmed working and restored; the duplicate "Shakira Stadium" row (PR #353 Discovery quirk, dead link) was deleted (31→30). PR #472 then re-added a "Shakira Stadium" Madrid row as `needs_recheck` (30→31; CTA-suppressed — see Active risks). |
| raye | 2026-06-19 | 0 | 0 | 0 | 0 | — | No event records; artist-level CTA only. |
| charli-xcx | 2026-06-18 | 11 | 11 | 11 | 0 | Music, Fashion, Film Tour | All 11 events `machine_high_confidence` with full SeatGeek coverage; titles backfilled 2026-07-06. |
| tate-mcrae | 2026-06-19 | 0 | 0 | 0 | 0 | — | No event records; artist-level CTA only. |
| summer-walker | 2026-06-11 | 13 | 7 | 1 | 0 | Still Finally Over It | Titles backfilled 2026-07-06 on the 3 newer events (Bristow is the "Broccoli City 2026" festival). Houston 6/21 storefront owner-verified (page loads; not on sale via TM) and restored with a plain "Check Ticketmaster" link 2026-07-15; 0 `needs_recheck`. |
| rosalia | 2026-06-18 | 6 | 1 | 0 | 0 | LUX TOUR 2026 | Titles backfilled 2026-07-06 across all 6 events. Houston 6/23 storefront owner-verified (page loads; not on sale via TM) and restored with a plain "Check Ticketmaster" link 2026-07-15; 0 `needs_recheck`. |
| post-malone | 2026-07-15 | 5 | 0 | 0 | 0 | — | Promoted 2026-07-15 (PR #466); 5 events landed via the discovery batch PR #472 (`tour_name` blank pending human verification; 13 recognised rows withheld). Vivid Seats covers 3 events; no SeatGeek event URLs yet. |
| zach-bryan | 2026-07-15 | 15 | 4 | 4 | **5** | — | Promoted 2026-07-15 (PR #466); 15 events landed via the discovery batch PR #472 (`tour_name` blank pending human verification). 5 short-form-URL rows are `needs_recheck` (Arlington, Glendale, Dover ×2, Auburn); 4 of the 5 publish a standalone SeatGeek CTA via verified provenance (PR #482), Auburn shows the safe no-link state. Vivid Seats covers 10 events. |
| sabrina-carpenter | null | 0 | 0 | 0 | 0 | — | `review_required` shell (PR #465): noindex, no CTA, `verified_providers: []`, no registry entry. Held pending live dates (0 upcoming on SeatGeek/TM at capture). |
| lady-gaga | null | 0 | 0 | 0 | 0 | — | `review_required` shell (PR #465): noindex, no CTA, `verified_providers: []`, no registry entry. Held pending live dates (Mayhem Ball concluded; 0 upcoming). |

Note on `needs_recheck` rendering (updated 2026-07-13): event CTAs publish independently per provider. Ticketmaster follows the row verification status. SeatGeek or Vivid Seats may still publish on a `needs_recheck` row when that provider link carries its own verified provenance. The shared rule is `providerEventPublishable` in `functions/api/out.js`, `functions/[[path]].js`, `public/app.js`, and `functions/api/shows.js`; the smoke suite guards SSR/API parity. Today (2026-07-16) 13 of 20 recheck rows retain SeatGeek coverage (olivia-rodrigo 6, zach-bryan 4, bts 2, ed-sheeran 1), 0 retain Vivid Seats coverage, and 7 show the safe no-link state (bts Madrid ×2, bad-bunny Marseille + Brussels, shakira Madrid, ed-sheeran Nashville, zach-bryan Auburn).

## Validation pipeline

Run before committing data, content, or rendering changes:

- `python3 scripts/validate-events.py --for-production` — event schema, hard error on missing `tour_name` key, warning on blank `tour_name` for indexed artists (PR #186).
- `node scripts/validate-guide-routes.mjs` — guide route / content / sitemap drift validation (PR #184).
- `node scripts/validate-artist-provider-claims.mjs` — artist metadata vs `VERIFIED_TICKET_LINKS` drift guard (PR #185).
- `node scripts/validate-cta-provider-state.mjs` (`npm run validate:cta-provider-state`, in `test:mvp`) — read-only CTA ↔ provider-state guard: artist CTAs backed by a verified registry identity, no withheld identity publishing, every publishable event resolvable through `/api/out`, every `machine_high_confidence` row meeting its canonical contract, and every verified provider provenance row carrying a matching redirect-valid provider URL.
- `npm run validate:internal-links` (`node scripts/audit-internal-links.mjs --check`, in `test:mvp`) — read-only in-process crawl of every HTML route: fails on orphaned indexable pages, canonical drift, robots/indexability disagreement, duplicate titles/descriptions, broken legacy guide redirects, and sitemap/indexability mismatches. `npm run audit:internal-links` writes the full page-by-page report to `reports/internal-links/`.
- `npm run seatgeek:verify:self-test` — offline invariant tests for the SeatGeek verification script (instant matching, wrong-night rejection, decision matrix, write shape). Run before changing `scripts/verify-seatgeek-events.mjs`.
- `npm run artist:check -- <slug>` — per-artist readiness validator: checks `artists.json`, `catalog.json`, `events.json`, partition files, `VERIFIED_TICKET_LINKS` in `out.js`, and the `shows.js` affiliate map (PR #188; since 2026-06-11 the shows.js map is derived from out.js and the signup allowlist from artists.json — neither is hand-edited per artist).
- `node scripts/smoke-prelaunch.mjs` — route/CTA/copy smoke checks.
- `npm run seatgeek:self-test` — SeatGeek discovery scoring/safety smoke test (no API calls). Run before changing the discovery tooling.
- `npm run providers:identities:validate` — provider identity registry validation (`data/provider-identities.json` vs `artists.json` and the out.js Ticketmaster host allowlist). Run whenever the registry changes. See `docs/PROVIDER_SYNC.md`.
- `node --check public/app.js`, `node --check 'functions/[[path]].js'`, `node --check functions/api/out.js` — syntax.
- `git diff --check` — whitespace and conflict markers.

`npm run test:mvp` runs documentation validation, the events/outbound-link self-tests, provider validators, CTA-provider-state guard, internal-link/indexability regression check, and smoke suite together.

## Daily automation

`.github/workflows/daily-audit.yml` runs at 03:00 UTC and on `workflow_dispatch`. It performs:

1. URL liveness via `scripts/verify-outbound-links.mjs`.
2. Ticketmaster Discovery diff via `scripts/audit-tm-events.mjs` (requires `TICKETMASTER_API_KEY`; skipped safely if absent). The Discovery API is keyed by the **Discovery event id** (`ticketmaster_discovery_event_id` / `provider_links.ticketmaster.discovery_event_id`), NOT the consumer-website `/event/<id>` code stored in `ticketmaster_event_id` (16-char hex) or the international numeric storefront id. Events lacking a Discovery-format id are reported as **`unresolvable`** (surfaced for backfill, **not** counted as missing). `scripts/backfill-discovery-ids.mjs` (`npm run audit:backfill-discovery-ids`, dry-run by default; `--apply` to write) recovers Discovery ids from the verified per-artist attraction feed by venue-local-date + city match, writing only on unambiguous API matches. Date/venue/status comparison is timezone-aware and only flags actionable status transitions (cancelled/postponed/rescheduled).
3. Reporting via `scripts/daily-audit-report.mjs` into a single rolling GitHub issue (`automation:daily-audit`).
4. Verification-date bumps via `scripts/bump-verified-dates.mjs`, opening a PR for human review. The TM-skip/failure guard added in PR #182 prevents date bumps when TM data is unavailable.

`.github/workflows/tm-new-shows-pr.yml` runs at 04:00 UTC and on `workflow_dispatch`. It is the scheduled **new-show discovery loop** (provider-sync step 3, write-to-PR mode): it runs the Ticketmaster Discovery recogniser for every verified, `sync_enabled` artist via `scripts/sync-tm-events-write-pr.mjs`, opens **one PR** (`automation:tm-events` label) of newly-discovered shows, and **auto-merges it (squash) once the in-run validation suite has passed** (owner-approved 2026-07-07; a failed merge leaves the PR open for a human). It never commits to `main` directly. The recogniser withholds events already in `events.json`, so only genuinely new shows are proposed and a quiet day produces no commit and no PR; risky rows are withheld for human review and never auto-merged; `event_name` is populated verbatim from the Discovery API listing title; `tour_name` is left blank for human verification (#172). Manual `workflow_dispatch` defaults to a safe `preview` (no PR), accepts an optional single-artist `artist` input, and only auto-merges when the `auto_merge` input is set. Without `TICKETMASTER_API_KEY` the recogniser no-ops safely (no rows, no PR). See `docs/PROVIDER_SYNC.md`.

`.github/workflows/nightly-data-sync.yml` runs at **03:30 UTC** (cron re-enabled 2026-07-07, owner-approved, after a clean local dry-run: 0 errors, 17 pending lossless updates, 80 review-only items) and on `workflow_dispatch`. Manual runs default to `dry_run: true`; a dry-run writes `.audit/tm-sync.json` as an uploaded artifact and cannot commit or push. A non-dry-run can **auto-commit to `main`** only for lossless factual updates (date/time, venue/city, the official listing title `event_name`, refreshed canonical TM URL) to events that already exist in `events.json`, pulled per event id from the Ticketmaster Discovery API via `scripts/apply-tm-updates.mjs`, then regenerates the inline fallback and partitions. The script only applies updates to events with zero review blockers of their own; the commit is blocked by any error, missing report, dry-run input, validation failure, smoke-test failure, or absent `events.json` diff (review items and blocked updates on other events go to the rolling issue without vetoing the clean updates — the old repo-wide zero-review gate could never pass given the permanently-offsale rows). Items that need human judgement — new shows, deletions (404/410), cancelled/postponed status, `tour_name` — are **never auto-applied**; they are surfaced in the rolling `automation:data-sync` issue via `scripts/report-tm-sync-review.mjs`. See `docs/PROVIDER_SYNC.md` and `SAFE_PUBLISHING_RULES.md` → "Discovery, Enrichment, and Rendering" for the boundary. (`TICKETMASTER_API_KEY` is required for useful live checks; if absent, the run writes a skipped report and no-ops safely.)

`.github/workflows/seatgeek-cta-sync.yml` runs at **05:00 UTC** (added 2026-07-08, owner-approved) and on `workflow_dispatch` (manual runs default to a safe preview). It is the SeatGeek CTA enrichment + verification loop: `scripts/enrich-seatgeek-events.mjs --apply-high-confidence` adds missing event-level `seatgeek_url` values for Ticketmaster-verified events (so newly-discovered shows get SeatGeek CTAs the morning after they land), then `scripts/verify-seatgeek-events.mjs --apply` confirms stored SeatGeek links against the SeatGeek API record for the registry-verified `seatgeek_performer_id` (UTC-instant match ±3h, city and venue match, event-URL shape) and writes `provider_links.seatgeek` verified provenance — which un-suppresses the SeatGeek CTA on `needs_recheck` events. It discovers URLs for suppressed events via a performer-id-scoped ±12h window query, corrects wrong-night URLs to the single unambiguous API match, and un-verifies/clears listings only on positive evidence (confirmed-gone 404/410 or confirmed mismatch, plus a successful zero-candidate discovery query); transient API failures leave events untouched, and auth/config failures (401/403) abort the run with exit 1 and no writes. Ambiguous matches are reported in the committed provider reports (`reports/provider-sync/seatgeek-cta-auto-add.md`, `reports/provider-sync/seatgeek-cta-verify.md`), never guessed. The full validation suite runs in-job before the PR opens; scheduled runs auto-merge (squash) via `scripts/open-automation-pr.mjs` `PR_AUTO_MERGE`, and a failed merge leaves the PR open for a human. Without `SEATGEEK_CLIENT_ID` the run no-ops safely. Only future events are maintained (past shows are skipped — SeatGeek delists them, and this keeps the smoke suite's pinned Gainesville canary untouched); events whose `datetime_iso` is date-only (time TBA) or timezone-naive with no IANA `timezone` field are skipped as date-ambiguous (never guessed).

`.github/workflows/vividseats-cta-sync.yml` runs at **05:30 UTC** (cron enabled 2026-07-12, owner-directed all-shows automation) and on `workflow_dispatch` (manual runs default to a safe preview). Its first reviewed data PR merged 218 verified event destinations; scheduled runs auto-merge after the in-run validation suite, mirroring `seatgeek-cta-sync.yml`. Monitor scheduled runs via the auto-merge PRs and `reports/provider-sync/vividseats-cta-sync.md`.

`.github/workflows/impact-marketplace-provider-sync.yml` is **scheduled nightly** since PR #480 (2026-07-15, owner-approved): TicketNetwork at **06:00 UTC**, Ticket Liquidator at **06:30 UTC**, StubHub International at **07:00 UTC**, serialized by a shared concurrency group so catalogue writes cannot race. Scheduled runs apply only unambiguous exact-event matches, open one provider PR, and **auto-merge after the full in-job validation suite passes**. Manual `workflow_dispatch` remains preview-first and a manual apply opens a review-only PR (never auto-merges). Event links only — no prices or availability claims; incomplete catalogues preserve existing links.

`.github/workflows/impact-marketplace-price-snapshots.yml` runs every 4 hours (cron `17 */4`), on pushes to `main` touching the runtime/provider contract, and on `workflow_dispatch` — exact-ID D1 snapshots for the approved numeric-price lanes (TicketNetwork, StubHub International). `.github/workflows/vividseats-price-snapshots.yml` runs every 4 hours (cron `47 */4`) and on dispatch. `.github/workflows/seatgeek-price-snapshots.yml` is **dispatch-only** (schedule removed 2026-07-15 — see Active risks). `.github/workflows/bootstrap-provider-pricing-schema.yml`, `.github/workflows/tm-data-refresh-pr.yml`, and `.github/workflows/seatgeek-discovery-proposal.yml` are manual-dispatch-only; none auto-merges.

Direct-to-`main` capability exists in exactly two workflows: `nightly-data-sync.yml` (gated lossless auto-commit described above) and `impact-marketplace-price-snapshots.yml`/`vividseats-price-snapshots.yml` write only to D1, never to the repo. Auto-merge-capable workflows: `tm-new-shows-pr.yml`, `seatgeek-cta-sync.yml`, `vividseats-cta-sync.yml`, and (scheduled runs only) `impact-marketplace-provider-sync.yml` — each only after its in-run validation suite passes.

`.github/workflows/prelaunch-validation.yml` includes a `stale-sync-guard` that runs `npm run events:sync` on PRs touching `public/data/*.json` and fails if `public/index.html` is stale.

## Active risks

These are the live risks. Detailed task scope and ordering live in `BACKLOG.md`.

- **New Impact provider lanes are active; event ingestion is now scheduled with auto-merge (PR #480, 2026-07-15, owner-approved).** TicketNetwork campaign `2322` / catalog `896`, Ticket Liquidator `2085` / `1315`, and StubHub International `24092` / `17571` were verified through the existing SeatGeek-scoped Impact account. The release includes campaign-isolated provenance, allowlisted tracked redirects, SSR/client CTAs, cache-only D1 snapshots, and health diagnostics. Nightly scheduled syncs (06:00/06:30/07:00 UTC) apply unambiguous exact-event matches and auto-merge after the in-job validation suite; manual dispatch stays preview-first with review-only apply. First scheduled TicketNetwork run merged 2026-07-16 (PR #484). TicketNetwork and StubHub International refresh six-hour price snapshots every four hours. Explicit false flags remain emergency kill switches. Ticket Liquidator prices stay disabled while its catalog has no numeric `CurrentPrice`; StubHub International remains separate from StubHub US/Canada.

- **Dedup-deleted rows can be re-proposed by new-show discovery.** The 2026-07-15 dedup deleted the dead "Shakira Stadium" Madrid row and the dead Bad Bunny Brussels `.com` row, but deleting them removed them from `events.json`, so the 04:00 UTC recogniser saw them as new shows and PR #472 re-added both the same day (as `needs_recheck`, short-form URLs — CTA-suppressed, so no unsafe link renders). Until deletions are recorded somewhere the recogniser can consult (or the rows are owner-resolved instead of deleted), expect deleted-but-still-listed TM shows to reappear as `needs_recheck` rows after each dedup.

- **Affiliate-pivot owner follow-ups.** Event-level Vivid Seats activation is complete and three owner spot-checks succeeded. The Vivid CTA and price crons are enabled and the inspected price run wrote every eligible row. Remaining work: confirm the existing Ticketmaster/SeatGeek redirect checks, delete unused `IMPACT_TICKETMASTER_*` secrets, and monitor scheduled price summaries for partial or zero-row runs. Artist-level Vivid Seats remains separate scope. The 2026-07-10 liveness audit's Vivid HEAD 404s are not sufficient evidence of dead links; the verifier requires GET confirmation.

- **SeatGeek price snapshots — permanently disabled (owner-confirmed 2026-07-15).** SeatGeek's API does not expose event pricing statistics for this client and never will: the inspected runs had both `SEATGEEK_CLIENT_ID` and `SEATGEEK_CLIENT_SECRET`, fetched every eligible exact-event listing with HTTP 200, and received null for every pricing-stat field (`lowest_price`, `average_price`, `highest_price`, and related), so no usable snapshot can be built. This is a permanent provider-API limitation, not a pending entitlement. The scheduled trigger in `seatgeek-price-snapshots.yml` has been removed (only `workflow_dispatch` remains, as a reversible escape hatch should SeatGeek ever ship pricing-stat access). `SEATGEEK_PRICE_DISPLAY_ENABLED` may be left `true` (owner-confirmed 2026-07-15 — `/api/health` reports it on) but is **inert**: the display gate only renders a snapshot when a fresh cached row exists, and none are ever written because the API returns null pricing stats, so no SeatGeek price ever shows regardless of the flag. No owner action is outstanding; SeatGeek remains a CTA-only provider (event links and artist performer pages), and price comparison is served by Vivid Seats, TicketNetwork, and StubHub International.
- **Daily TM audit — false "missing" storm fixed; residual real findings.** The audit was reporting ~314/339 events "missing (404/410)" because it queried the Discovery API with the website `/event/<id>` hex code / international numeric id instead of the Discovery event id. Fixed: the audit now keys on `ticketmaster_discovery_event_id`, and 218 events were backfilled with verified Discovery ids (`scripts/backfill-discovery-ids.mjs`). Post-fix the diff is **0 missing, 4 changed, 50 unresolvable**. The **4 changed** are genuine venue-label discrepancies (Olivia Rodrigo Amsterdam 2027: local "Vinyl Room - Ziggo Dome" vs TM "Ziggo Dome Club") — review and correct the local label or accept TM's. The **50 unresolvable** are European/`announced` shows not surfaced by id in the US Discovery feed (mostly Bad Bunny EU, BTS, Morgan Wallen announced dates); they carry verified storefront URLs and are CTA-safe — left without a Discovery id rather than guessing. NB: in CI the link-liveness half still returns ~all WAF-blocked, and the conservative blocked-link guard holds date bumps regardless — that is intentional (commit "Keep date-bump guard conservative for blocked links") and unchanged here.

- **Duplicate event rows — resolved 2026-07-15 (owner-approved dedup).** Three duplicate clusters were found and cleared. (a) **13 Ariana Grande** shows each had one legacy hex-id row (`human_verified`) and one Discovery-id row (`machine_high_confidence`) with the *same* `discovery_event_id`/`ticketmaster_event_id`/TM URL — the new-show automation re-added an existing show under a Discovery-id-suffixed `id`. (b) **3 Bad Bunny** shows (Düsseldorf 6/20, 6/21; Brussels 7/21) each had a dead `.com` `needs_recheck` row duplicating an already-published `human_verified` `.de`/`.be` row. (c) **1 Shakira** "Shakira Stadium" 9/27 row (PR #353 Discovery quirk) duplicated the working 9/27 residency row. In every case the redundant/dead copy was deleted and the row that carried the working CTA + resale coverage was kept, so no show lost a CTA and the surviving rows keep the `discovery_event_id` the nightly sync matches on. Site total 397→380 (Ariana 54→41, Bad Bunny 28→25, Shakira 31→30). The earlier wrong-night SeatGeek URL shares were separately corrected by PR #356.
- **Ticketmaster storefront rechecks — 20 `needs_recheck` as of 2026-07-16.** The owner's 2026-07-15 browser check took the count 36→13 (19 confirmed-working URLs restored to `human_verified` — BTS Munich 7/11 & 7/12; all 12 Shakira Madrid residency dates; Ariana Sunrise 7/1/3/4, Summer Walker Houston, ROSALÍA Houston restored as plain "Check Ticketmaster" links; 5 confirmed dead cleared — BTS Madrid 6/26 & 6/27, BTS Arlington 8/16 & 8/17, Bad Bunny Marseille 7/1). PR #472 then added 7 more recheck rows (zach-bryan 5 short-form URLs, plus the re-added shakira and bad-bunny rows above), taking the total to **20: olivia-rodrigo 6, zach-bryan 5, bts 4, bad-bunny 2, ed-sheeran 2, shakira 1**. The Discovery API resolving an id is not sufficient evidence to restore a storefront URL. Provider-specific resale provenance is independent: 13 recheck rows publish SeatGeek (olivia-rodrigo 6, zach-bryan 4, bts 2, ed-sheeran 1), 0 publish Vivid Seats, and 7 have no approved resale provider and show the safe no-link state.

- **Current-state counts still require a deliberate recount.** `npm run docs:check` protects canonical paths, relative links, and documented commands, but it cannot prove data counts or production flags. Any provider, event, artist, guide, workflow, or runtime-config change must refresh this file from source data and `/api/health`.

## Product guardrails (summary)

The full rules are in `docs/CONTENT_RULES.md`, `docs/PROVIDER_DATA_POLICY.md`, and `CLAUDE.md`. Highlights:

- Never invent tours, dates, venues, prices, availability, providers, inventory, or ticket links.
- Never scrape ticket providers.
- Never claim live price comparison unless approved provider feeds support it.
- Never expose Impact or any other secret client-side.
- CTAs only when the artist/event is reviewed, the provider destination has the required verification/provenance, and the URL, runtime configuration, and `/api/out` gates pass.

## What is supported today

- Homepage, the `/compare-concert-ticket-prices` comparison hub, and trust/legal pages.
- Artist index plus all 18 indexable artist pages above (3 of them — beyonce, raye, tate-mcrae — currently have no event records and render artist-level CTAs only), plus 2 `review_required` shells (sabrina-carpenter, lady-gaga) that render `noindex` with no CTA and accept watchlist signups.
- **Venue landing pages** (`/venues` index + `/venues/<slug>`): a server-rendered aggregation layer derived purely from verified `events.json` records (`functions/_venues.js`, shared with the sitemap). Each venue page lists the upcoming tracked shows at that venue grouped by artist and links out to the artist pages, where the verified provider CTAs and price snapshots live — the venue layer invents no data and has no provider/CTA logic of its own. Slug is `slugify("<venue> <city>")` so inconsistent country labels for one physical venue merge. Venues with ≥2 upcoming shows are indexable and in the sitemap; single-show venues render but stay `noindex`; unknown slugs 404. Cross-linked from the header and footer nav. As of the 2026-07-16 recount: 77 venues carry ≥1 upcoming tracked show, of which 53 are indexable (≥2 upcoming shows); counts move with `events.json` and the calendar.
- 17 guide pages with server-rendered content, each carrying a curated "Related guides" cross-link section.
- Verified SeatGeek CTAs, **artist-level and event-level**, as the primary CTA — routed through `/api/out` with Impact tracking (base tracking URL or API mint). Artist-level entries are performer-page URLs captured from the SeatGeek `/2/performers/{id}` API for registry-verified performer ids. Currently 250/402 events carry a stored `seatgeek_url` and 198 carry verified SeatGeek provenance; enrichment + identity-anchored verification run nightly via `seatgeek-cta-sync.yml` (first run merged 2026-07-08, PR #356; see Daily automation), including the standalone SeatGeek CTA path for `needs_recheck` events (13 live today); see `docs/SEATGEEK_DISCOVERY.md`.
- Verified plain Ticketmaster links (artist- and event-level) — unmonetized, rendered after the affiliate providers and labeled "Check Ticketmaster". No Impact wrapping, no Publisher Tag, no evyy.net shortlinks.
- **Every monetized CTA (SeatGeek, Vivid Seats, TicketNetwork, Ticket Liquidator, StubHub International), artist- and event-level, routes through `/api/out` for server-side Impact tracking** — never a raw affiliate URL in the page. `artistProviderHref`/`eventTicketHref` (client `public/app.js` and SSR `functions/[[path]].js`) emit `/api/out?...&provider=<slug>`, which `out.js` resolves and Impact-wraps (verified live: `seatgeek.pxf.io`, `vivid-seats.pxf.io`). The account Impact Publisher Tag (`public/impact-publisher-tag.js`, UTT `P-A3977745`) loads site-wide for **impression** tracking only (`impactStat("trackImpression")`); it does not transform links, so click attribution does not depend on client-side rewriting or Impact dashboard auto-link config. Do not switch monetized CTAs back to raw/direct destinations.
- Verified Vivid Seats CTAs at event level: 222 verified destinations are live and Impact-wrapped. (No `needs_recheck` rows carry standalone Vivid coverage.) Artist-level Vivid Seats entries remain unsupported; the sync workflow runs nightly at 05:30 UTC.
- Active TicketNetwork, Ticket Liquidator, and StubHub International event lanes: 198, 156, and 200 verified event links respectively, with exact-match provenance and server-side Impact tracking; nightly scheduled syncs auto-merge under their gates since PR #480. TicketNetwork and StubHub International support cache-only exact-ID listed-price snapshots; Ticket Liquidator price display is disabled because the current feed has no numeric `CurrentPrice`.
- Batch onboarding tooling: `npm run artists:onboard:propose` (API-captured SeatGeek/TM identity manifest) and `npm run artists:promote:batch` (up to 20 shells per PR, per-artist human browser spot-check checklist, dry-run by default).
- First-party analytics and signup writes through `DEMAND_DB`.
- Provider sync uses validated PR workflows: Ticketmaster new-show, lossless field sync, SeatGeek CTA sync, Vivid Seats CTA sync (since 2026-07-12), and the Impact marketplace event-link sync (since 2026-07-15, PR #480) may auto-merge only within their sanctioned gates. Runtime CTA gates and `/api/out` remain fail-closed.
- Approved price snapshots for every eligible show: `/api/shows` serves a bulk `priceProviders=approved-marketplaces` lane (cache-only batched D1 read — never a provider call), and both SSR and client rendering use those cached rows. Display remains gated per provider by the Cloudflare feature flags, approved source, exact provider-level event verification, valid timestamps, and expiry.

## What is not supported

- Live inventory aggregation and "cheapest" / "guaranteed availability" claims. Approved lanes are provider-listed snapshots rather than live checkout totals; each remains fail closed unless its rights, URL, exact-event, source, timestamp, and expiry gates pass.
- Tour, city, or event landing pages. (Venue landing pages **are** now supported — see "What is supported today".)
- Artist-level Vivid Seats CTAs. Event-level Vivid Seats CTAs are supported and live.
- Ticket Liquidator listed-price snapshots while its Impact catalog omits numeric `CurrentPrice`. StubHub US/Canada is not part of the StubHub International integration.
- Ticketmaster affiliate/tracking of any kind (the site is no longer in the Ticketmaster affiliate programme).
- `Event` / `MusicEvent` schema on any page without verified event-level data.

## How to update this file

Refresh when any of the following change: artist count, indexing status, event count, guide count, bindings, validation pipeline, daily automation, or the set of active issues. Recount from the data files (`python3 -c` over `public/data/*.json`) rather than trusting prior text — this file drifted once already. Reference `BACKLOG.md` for prioritised work — do not duplicate the priority list here. See `docs/DOCS_MAINTENANCE.md` for the canonical-file map.
