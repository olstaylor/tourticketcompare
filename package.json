{
  "name": "beyonce-ticket-compare",
  "version": "1.0.0",
  "description": "Minimal MVP for comparing ticket prices for Beyonce tour dates (Cloudflare Pages + Functions).",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2025-01-01 --port 3000",
    "deploy": "wrangler pages deploy public",
    "events:csv": "python3 scripts/csv-to-events.py",
    "events:sync": "python3 scripts/sync-events-data.py",
    "events:validate": "python3 scripts/validate-events.py",
    "events:update": "python3 scripts/csv-to-events.py && python3 scripts/validate-events.py && python3 scripts/sync-events-data.py"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
