# PROJECT_STATUS narrative, 2026-07-31 → 2026-08-04

Frozen 2026-08-04 (documentation-consolidation pass). This is the verbatim `Latest change` / `Earlier change` chain that had accumulated at the top of `PROJECT_STATUS.md` since the previous freeze (`2026-08-04-project-status-narrative.md`, which covers 2026-07-22 → 2026-08-01), moved here under the lifecycle rule in `docs/DOCS_MAINTENANCE.md` so that file can stay current-state only. Every live residue — counts, gates, active risks — was re-derived from source data and lives in `PROJECT_STATUS.md` / `docs/OPERATIONS.md`; nothing here is current-state authority.

Chain head: **Last updated: 2026-08-04 (agent — documentation refresh against live state)**.

- **2026-08-04 — full-board pricing, `/api/impact/*` token-gating, content editor origin isolation, Google tag fix.** (1) **Full-board pricing:** the router previously attached cached price snapshots to a fixed six-show prefix of each board while server-rendering the whole board, so a card past the sixth date could neither show a snapshot nor honestly report one as absent even where D1 held a fresh row — 74 of 78 cards on `/artists/olivia-rodrigo` were blank for no-JS visitors and crawlers. Each route now queries exactly the cards it renders (reads batched at 50 ids, so the largest board costs 2 cache reads rather than 84); coverage on that board went 4 priced / 74 silent → 74 priced / 4 explicitly noted. A card states the unavailable case only where its lanes were actually checked. (2) **All four `/api/impact/*` diagnostics are `DEBUG_API_TOKEN`-gated** and 404 without the token; `/api/health` reports the same gate it describes; a proxied 404 is now treated as an auth failure rather than an empty catalog. (3) **The `/admin` content editor moved to `admin.tourticketcompare.com`** — the middleware serves the editor on that host only and 404s every editor path on the apex, enforced by the smoke suite. The custom domain is not yet created, so the editor is currently unreachable (fail-closed, not a regression). (4) **Google tag fix:** the duplicate `gtag.js` loader is gone from the shell; GTM loads GA4 and the page pushes one `ttc_google_tag_init` event, with the CSP script hash and `connect-src` updated accordingly. (PRs #646/#652/#648 and related.)

- **2026-08-03 — site-wide copy provenance: published dates now derive from the copy itself.** Static-page "Updated" dates had drifted: 11 of 17 guides published 17–19 Jun while `sitemap.xml.js` served every trust route a hardcoded `STATIC_LASTMOD`, and the footer copyright year was client-JS-only. New `scripts/sync-content-provenance.mjs` fingerprints each of the 27 static routes over the copy that actually renders and advances `lastmod` only when that fingerprint changes; state lives in generated `data/content-provenance.json`, enforced by `content:provenance:check` in `test:mvp`. No published date moved on landing — every route seeded at the date it was already showing, so the mechanism starts tracking from the next real edit. Two rejected alternatives (stamping today on deploy; deriving from git history) are recorded in `docs/CONTENT_RULES.md`. Separately, `scripts/check-guide-source-links.mjs` (now in `daily-audit.yml`) re-checks cited guide source URLs and stamps a distinct `linkCheckedAt`, never overwriting the human-only editorial `lastChecked`. (PR #647.)

- **2026-07-31 — merge reconciliation: route-usefulness policy preserved alongside new commercial funnel measurement.** Authoritative outbound rows (`outbound_click`, written server-side by `/api/out`) can now join to visits and carry page/event/CTA/provider context; same-origin referrers restore clicked-page attribution without exposing cross-origin referrers; blocked outbound attempts are recorded separately; analytics no longer accepts client-supplied email; the new commercial-funnel report avoids presenting checkout conversion or revenue the site cannot observe (migration `0008` additive, not yet applied — writers fall back by schema tier). Landed the same day: the ten-artist `review_required` Shell batch (karol-g, foo-fighters, metallica, rush, muse, my-chemical-romance, teddy-swims, five-finger-death-punch, system-of-a-down, laura-pausini) and the latest provider-sync state, plus an owner-reviewed storefront and tour-label cleanup — five official Ticketmaster links restored, one duplicate Shakira Madrid row removed and tombstoned, seven confirmed tour mappings applied to 217 rows. Event count 608→607; `human_verified` 284→289; `needs_recheck` 52→46.
