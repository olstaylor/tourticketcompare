# PROJECT_STATUS narrative, 2026-07-22 → 2026-08-01

Frozen 2026-08-04. This is the verbatim `Last updated:` chain that had accumulated in `PROJECT_STATUS.md` as one paragraph, moved here under the lifecycle rule in `docs/DOCS_MAINTENANCE.md` so that file can stay current-state only. Every live residue — counts, gates, active risks — was re-derived from source data, `/api/health` and production D1 on 2026-08-04 and lives in `PROJECT_STATUS.md`; nothing here is current-state authority, and several figures below were already superseded on the day this was frozen.

Chain head: **Last updated: 2026-08-01 (agent)**.

- **blog + Markdown content pipeline**: blog posts are now authored as Markdown with YAML front matter in `content/blog/`, which is the source of truth; `scripts/build-blog-content.mjs` compiles them into the single generated asset `public/data/blog-content.json` that the runtime reads. The compile exists because Cloudflare Pages serves `public/` with no build step and Pages Functions cannot list a directory, so nothing else could discover the files; `.github/workflows/content-build.yml` runs it on pushes touching `content/blog/`, gates it behind the full validation suite, and commits the result — that commit is what deploys a post. `npm run blog:check` fails CI when the generated file drifts from its source, the same contract `stale-sync-guard` gives event data. New shared derivation `functions/_blog.js` owns the gates and is read by the router, sitemap, `llms.txt`, the new RSS feed at `/blog/rss.xml`, and both site audits, so the blog's indexable URL set cannot drift between them: a `draft` post has no route at all (404), a published post is indexable at 300+ body words, and a tag page is indexable once **two or more indexable** posts share the tag — below either threshold the page still returns 200 with a self-referencing canonical and stays linked, `noindex,follow`, exactly as single-date artist-city pages do (`docs/ROUTE_INDEXABILITY_POLICY.md`). Post pages emit `BlogPosting`, index and tag pages `Blog`/`CollectionPage` with a nested `ItemList`. The blog carries no provider, CTA, price, or event logic. Three seed posts shipped, all describing the site's own published behaviour and citing no external data. The build validator rejects a post with a broken internal link, an over-budget title or description, an embedded image (the renderer has no image path), or a claim the site cannot support — it fired on the agent's own draft copy during authoring, which is the intended behaviour. Alongside it a browser content editor at **`/admin`** (Sveltia CMS 0.176.0, vendored into `public/admin/sveltia-cms.js` so no third-party host appears in the CSP of a page that can hold a repository token) commits Markdown through the signed-in editor's **own** GitHub account: it holds no site credential, is `noindex` and `robots.txt`-disallowed, serves its own tightened CSP, refuses non-canonical hosts, and returns a 503 with setup instructions until the owner creates the OAuth app (see Active risks and `docs/BLOG.md`). Indexable surface 220 → 227 routes (+1 blog index, +3 posts, +2 tags, +1 venue from ordinary data movement); baseline re-anchored deliberately.

- Earlier 2026-07-31: **artist-page UX and content pass**: artist pages now lead with a data-grounded summary of their own board and a fact strip, then the dates, provider options, one shared price/fee help component, and a full editorial-provenance block; three overlapping generic copy blocks were consolidated into that one component, show cards gained local time / country / prominent multi-night night numbers, long boards gained a no-JS month jump list, empty boards lost the generic buying advice, and hydration transplants the server-derived copy rather than duplicating it. Detail in “What is supported today”.

- Earlier 2026-07-31: **route-usefulness policy: indexable surface 311 → 177 routes (-43%)**. A pre-change baseline (`reports/status-history/2026-07-30-indexable-surface-baseline.md`) found that 210 of 311 indexable routes were artist-city pages — a route type with **0 recorded views since 1 June** — and that 128 of those 210 carried a single upcoming date, making them the artist page filtered to one show card. New shared module `functions/_route-indexability.js` is now the single source of truth for every dynamic gate (thresholds, event publishability, exclusion reason codes). It exposes two deliberately distinct tests: `eventPublishable` ("can this page lead anywhere?", drives indexability) and `eventStatusPublishable` ("does this event get a `MusicEvent` node?", the pre-existing row-status contract, unchanged) — conflating them is what a first pass got wrong, read by `_cities`/`_venues`/`_artist-cities`, the router, sitemap, llms.txt, both audits and the forecast. Three changes: (1) an artist-city page is indexable only with **≥2 publishable upcoming shows** in the city — single-date combinations still render 200 with a self-referencing canonical, stay linked from their artist page, and are `noindex,follow`; **nothing was redirected** (238 rendered → 92 indexable, 146 noindex). (2) City and venue pages now additionally require **≥1 upcoming show with a publishable ticket destination**. "Can lead somewhere" means what the renderer means by it — a publishable row status **or** an independently verified marketplace destination with a stored URL — so a `needs_recheck` row publishing a standalone SeatGeek CTA counts (this is most of Arlington, Houston and Sunrise). On current data the requirement excludes **one venue page** (AFAS Dome Antwerp: 3 upcoming shows, no verified link from any provider) and **no city pages**. (3) Interchangeable content removed across all three location types: the generic ticket-buying FAQ entries that repeated verbatim on every page (and were mirrored into `FAQPage` on each), the templated "How to buy \\<artist\\> tickets in \\<city\\>" checklist, and the boilerplate city/venue buying checklists — all replaced by links to the guides that already cover them. `FAQPage` and `MusicEvent` are now emitted only on indexable location pages, so structured data follows indexability in both directions. New `npm run audit:indexable-surface` reports routes by type, exclusion reasons, routes about to lose indexability, orphaned indexable routes, duplicate/near-duplicate title patterns, routes with no future events, and change against a stored baseline; its `--check` mode is wired into `test:mvp` and `prelaunch-validation.yml` and **fails only on structural change** — routes that still render changing indexability — while ordinary event expiry is reported and a ≥25% total swing raises a non-blocking annotation. Policy: `docs/ROUTE_INDEXABILITY_POLICY.md`. Rollback is `git revert` of this change plus a baseline re-anchor; no data, redirects, `/api/out`, or affiliate logic were touched.

- Earlier 2026-07-31: Shell phase for a ten-artist touring-roster batch: karol-g, foo-fighters, metallica, rush, muse, my-chemical-romance, teddy-swims, five-finger-death-punch, system-of-a-down and laura-pausini added as `review_required` shells after owner browser checks. Artists 30→40 and catalog ticket links 56→66. All ten are noindex, carry no CTA or events, and add no indexable routes until the separate Promote and Events gates clear. Unannounced 2027 tours were not encoded or claimed.

- Earlier 2026-07-30: **price snapshot cadence 8h → 2h** on both numeric lanes, with `DEFAULT_FRESHNESS_HOURS` 10h → 6h to match, plus a new 90-day retention prune for `provider_pricing_history`. Rationale and the supporting drift measurement are in Daily automation → Price snapshot cadence; the short version is that the Actions-minutes reason recorded for the 2026-07-26 throttle did not apply to a public repo, and the history table shows listed prices drift ~2.3%/hour rather than sitting still between runs, so average displayed staleness falls 4h → ~1h. New `scripts/prune-provider-pricing-history.mjs` keeps the snapshot writers append-only (both self-tests assert they emit no `DELETE`/`UPDATE` against the history table) while bounding a table that nothing had ever pruned — 19.9k rows in its first 8 days, growing ~7.4k/day at the new cadence.

- Earlier 2026-07-30: data + automation correctness pass, follow-ups from PR #615. (1) Venue-local dates: all 47 events that carried a UTC instant with no `timezone` were rewritten to venue-local wall time WITH its UTC offset (`2026-11-19T19:00:00-05:00`), built from the `localDate`/`localTime`/`dateTime` of each event's own Ticketmaster Discovery record and asserted to denote the same instant TM reports. 32 of the 47 had been rendering a day late. The offset form is load-bearing in three ways: the venue's clock is written literally so the date renders correctly without a `timezone`, the exact instant survives for sorting and the upcoming-show filter, and `datetimeGenuinelyChanged` in `apply-tm-updates.mjs` takes its instant-comparison branch — a naive wall-time string would have been read as UTC and auto-reverted by the 03:30 nightly sync. Z-suffixed rows with no timezone: 47 -> 0. (2) International Ticketmaster ids: `/event/<slug>/<numeric-id>` storefront URLs (ticketmaster.de/.nl/.es/.be) had the shared event-name slug stored as `ticketmaster_event_id`, so every date on a tour collided — 9 gracie-abrams, 6 niall-horan and 2 bts pairs. `showIdentity`/`mergeShows` key on that field, so those dates collapsed into one show on the live-discovery path. Parser fixed in `sync-ticketmaster-events.py` and `validate-events.py` (kept in step), 20 existing rows repaired from their own stored URLs (the 2 bts Madrid rows carry no URL, so their ids came from the Discovery record). Colliding ids: 4 groups -> 0. (3) Discovery heartbeat: `report-tm-discovery-coverage.mjs` now treats a `write-pr` run that proposed events but opened no PR as a problem. Previously coverage.json was written before the in-run validation suite, so the 2026-07-30 06:20 failure that discarded 183 recognised events — including a JAY-Z London date already on sale — left the rolling issue green; the miss surfaced only because a visitor asked. Its self-test was also the last one not wired into `test:mvp`, and is now wired in. (4) Live-discovery merge identity: `showIdentity` keyed only on `ticketmaster_event_id`, but persisted rows hold the public storefront id while `mapTicketmasterEventToShow` holds Discovery's own id — different namespaces, so 506 of the 560 rows carrying both ids could never merge and the opt-in live path would return the same concert twice. Both show models now carry `ticketmaster_discovery_event_id` and `showIdentity` prefers it. Pre-existing and dormant (`TICKETMASTER_LIVE_ARTIST_DISCOVERY_ENABLED` defaults off). (5) Per-artist prose below refreshed for the five 2026-07-29 artists that received their first events on 2026-07-30; the zero-event indexable list is now beyonce/raye/tate-mcrae/latto (the count of 4 was already right, the list beside it was not).

- Earlier 2026-07-30 (fact correction only — an Actions-scalability review of workflow run history found the Daily automation table and Active risks still documented `impact-marketplace-price-snapshots.yml`/`vividseats-price-snapshots.yml` at their old every-4h cadence; commit `311bb29` (2026-07-26, "ci: cut Actions minutes and stop snapshot hangs") had already throttled both to every 8h, added missing per-job `timeout-minutes` caps across the scheduled lanes, and bounded Impact catalog requests with a 15s/3-attempt timeout to stop stalled snapshot runs from hanging to the job ceiling — the repo was right, the doc had drifted. Corrected the two Daily automation rows, the TicketNetwork/StubHub International Active-risks sentence, and the Phase-1 price-history cadence note; no other figures recounted this pass. Separately observed: 72–113 recent scheduled runs per workflow show high real-world reliability (single-digit-percent failure rate, mostly transient upstream timeouts that self-heal on the next tick — e.g. the Vivid Seats CTA sync `curl: (28) Operation timed out` on 2026-07-27), and the 2026-07-30 06:20 UTC `tm-new-shows-pr` failure was a genuine roster-growth scalability bug — the internal-link audit's SERP title-length budget (60 chars) was exceeded by two city pages and a new artist-city combination — already caught by validation and fixed same-day via PR #610.

- Earlier 2026-07-29: Entry-page treatment of empty boards — the homepage and `/artists` listings now derive their artist badges from the same `artistHasUpcomingShow` gate as the indexability decision, so the 16 of 30 artist pages with no upcoming dates are badged `No dates yet`, given a "Get date alerts" CTA, and sorted below the artists with dates instead of being linked as if they had tickets. Covers all three listing surfaces including the hydrated homepage (`public/ttc-home.js`), whose hero chips previously led with two dateless artists. Links stay `follow`; only prominence changed. Also corrected the stale editorially-indexable artist-page counts below — 20→26 indexable records, 14 of 26 live, 16 of 30 pages `noindex` — recounted with `npm run roster:forecast`.

- Earlier 2026-07-29: Promote phase for the six-artist batch — gracie-abrams, niall-horan, doja-cat, sombr, latto, john-summit are now `indexable_with_substantial_content` with `verified_providers: ["ticketmaster","seatgeek"]`, registry entries, and artist-level `<slug>:seatgeek` + plain `<slug>:ticketmaster` entries in `VERIFIED_TICKET_LINKS`. Indexable artists 20→26, `VERIFIED_TICKET_LINKS` 40→52, registry 20→26, catalog ticket_links 50→56. **All six carry 0 events, so their pages stay `noindex` until the nightly TM discovery lane lands dates — indexable route count is unchanged at 167.** All six pass the discovery eligibility gates. Destination URLs could not be machine-verified from CI (SeatGeek and Ticketmaster 403 datacenter IPs — confirmed against known-good live URLs), so the per-artist browser spot-check in the PR body is the only verification. `scripts/validate-status-counts.mjs`: the zero-event anchor no longer pins the indexable total or the slug list, which silently broke it when the roster grew.

- Earlier 2026-07-29: Shell phase for the same batch — gracie-abrams, niall-horan, doja-cat, sombr, latto, john-summit added as `review_required` shells: noindex, no CTA, `verified_providers: []`, unverified/hidden ticketmaster ticket_links rows, no `VERIFIED_TICKET_LINKS` or registry entries. Artists 24→30, catalog ticket_links 44→50; enforced figures refreshed via `npm run status:validate:write` and the surrounding prose corrected by hand, which the writer does not touch. Candidates came from the new `npm run roster:forecast:candidates`; identities are API-captured and still need the owner browser spot-check before Promote. Also recounted live artist pages 15 of 20 → **14 of 20** (post-malone empty-boarded when its last tracked date passed on 2026-07-29; 10 of 30 artist pages now render `noindex`), and quantified the indexable-surface decay in Active risks.

- Earlier 2026-07-28: IndexNow automation + hand-maintained-count recount: `indexnow-ping.yml` now submits the sitemap after deploys that change the indexable route set. Recounted the date-derived figures this file warns are not machine-enforced, all of which had drifted since 2026-07-22 — cities 52 known/13 indexable → **51/16**, venues 70/17 → **73/20**, artist-city combinations recorded for the first time at **90**, and live artist pages **15 of 20** editorially-indexable (9 of 24 total render `noindex`). Synced the unenforced provider prose in "What is supported today" to the enforced figures it had drifted from (SeatGeek 252/200 → 248/196, Vivid 239 → 238, TicketNetwork/Ticket Liquidator/StubHub International 203/172/209 → 202/171/206). New Active risk records that indexable surface has grown while audience has not.

- Earlier the same day — analytics/indexing correctness pass: non-canonical hosts — notably the permanent `tourticketcompare.pages.dev` production alias — now serve `noindex` + apex canonicals; first-party analytics gained external-referrer/UTM acquisition capture; self-identifying crawlers no longer write `outbound_click`/`page_view` rows. Data counts below unchanged and still current as of the 2026-07-23 recount.

- 2026-07-23: full recount from source data — the nightly sync lanes had added event/provenance rows since the 2026-07-22 edit without refreshing this file: events 402→423, `needs_recheck` 20→24, and provider provenance/per-artist figures moved with them; jelly-roll and tame-impala now carry event records. Corrected figures below. New `scripts/validate-status-counts.mjs` now recounts these against source and self-heals them in the sync lanes; city/venue counts remain date-derived and are not machine-enforced.

- 2026-07-22: city landing-page aggregation added and tightened to 52 known cities / 13 indexable under the ≥4 upcoming shows / ≥2 artists gate; jelly-roll + tame-impala batch-promoted — 20 indexable / 4 shells
